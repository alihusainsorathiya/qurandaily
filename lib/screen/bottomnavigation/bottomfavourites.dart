import 'package:flutter/material.dart';

class BottomFavourites extends StatefulWidget {
  const BottomFavourites({Key? key}) : super(key: key);

  @override
  State<BottomFavourites> createState() => _BottomFavouritesState();
}

class _BottomFavouritesState extends State<BottomFavourites> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
